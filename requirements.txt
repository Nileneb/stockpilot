Django>=5.1,<5.2
django-unfold
django-tenants
psycopg[binary]
Pillow
pytest
pytest-django
# Vision / ML — install torch from the CPU index for smaller footprint:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu torch torchvision
torch
torchvision
ultralytics
