[pytest]
DJANGO_SETTINGS_MODULE = stockpilot.settings.dev
python_files = tests.py test_*.py *_tests.py
testpaths = tests apps
addopts = -ra --strict-markers
markers =
    integration: hits real ML model (slow, may download weights)
